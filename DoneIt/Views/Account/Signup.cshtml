@model SignupViewModel

@{
    Layout = "SignIn/_SignInLayout";
}

<div class="container is-fluid">
    <section class="section">
        <div class="container has-text-centered">
            <div asp-validation-summary="All" class="has-text-danger"></div>
            <div class="columns is-centered">
                <div class="column is-5 is-4-desktop">
                    <h2 class="title">
                        Sign up
                    </h2>
                    <form method="post">
                        <div class="field">
                            <div class="control">
                                <input class="input" asp-for="Email" placeholder="Email" />
                                <span asp-validation-for="Email" class="has-text-danger"></span>
                            </div>
                        </div>
                        <div class="field">
                            <div class="control">
                                <input class="input" asp-for="Name" placeholder="Name" />
                            </div>
                        </div>
                        <div class="field">
                            <div class="control">
                                <input class="input" asp-for="Password" placeholder="Password" />
                                <span asp-validation-for="Password" class="has-text-danger"></span>
                            </div>
                        </div>
                        <div class="field">
                            <div class="control">
                                <input class="input" asp-for="PasswordConfirm" placeholder="Confirm Password" />
                                <span asp-validation-for="PasswordConfirm" class="has-text-danger"></span>
                            </div>
                        </div>
                        <div class="field is-grouped">
                            <div class="control is-expanded">
                                <button class="button is-primary is-fullwidth">
                                    Sign Up!
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </section>
</div>